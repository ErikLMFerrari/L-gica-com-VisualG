Algoritmo "AreaDoTriangulo"

Var
     B, H, A: real

Inicio
     Escreval("Escreva a altura do triangulo")
     Leia(H)
     Escreval("Escreva a base do triangulo")
     Leia(B)
     A <- (B * H) / 2
     Escreval("A area do triângulo é:", A)

Fimalgoritmo